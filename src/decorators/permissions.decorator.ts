// import { PermissionAction } from '@/api/permission/entities/permission.entity';
// import { SetMetadata } from '@nestjs/common';

// export const PERMISSIONS_KEY = 'roles';

// export const Permissions = (action: PermissionAction, entity: string) =>
//   SetMetadata(PERMISSIONS_KEY, [action, entity]);
